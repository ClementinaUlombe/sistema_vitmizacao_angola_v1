export default function DataFilterPage() {
  return (
    <div className="p-4">
      <h2 className="text-2xl font-semibold text-foreground mb-4">Filtrar Dados</h2>
      <p className="text-muted-foreground">
        Esta página é para filtrar dados (por idade, género, zona, ocupação, etc.).
      </p>
    </div>
  );
}
